"use client";import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e:any){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)setError(d.error);else router.push("/app")}return <main className="authPage"><form onSubmit={submit} className="authCard"><Link href="/" className="brand">✦ KIVU AI</Link><h1>Welcome back</h1><p>Continue your KIVU journey.</p><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/>{error&&<small className="formError">{error}</small>}<button className="button">Sign in</button><p>New to KIVU? <Link href="/signup">Create an account</Link></p></form></main>