import {NextRequest,NextResponse} from "next/server";import {readSession} from "@/lib/auth";import {prisma} from "@/lib/db";export async function GET(req:NextRequest){try{const t=req.cookies.get("kivu_session")?.value;if(!t)return NextResponse.json({user:null});const id=await readSession(t);const u=await prisma.user.findUnique({where:{id},select:{name:true,email:true}});return NextResponse.json({user:u})}catch{return NextResponse.json({user:null})}}