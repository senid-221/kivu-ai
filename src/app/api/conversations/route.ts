import {NextRequest,NextResponse} from "next/server";import {prisma} from "@/lib/db";import {readSession} from "@/lib/auth";async function uid(req:NextRequest){const t=req.cookies.get("kivu_session")?.value;if(!t)throw Error();return readSession(t)}export async function GET(req:NextRequest){try{const userId=await uid(req);return NextResponse.json({items:await prisma.conversation.findMany({where:{userId},include:{messages:true},orderBy:{updatedAt:"desc"}})})}catch{return NextResponse.json({items:[]})}}