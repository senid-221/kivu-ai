import {NextRequest,NextResponse} from "next/server";
const prompts:any={teacher:"You are KIVU Teacher. Explain clearly step by step.",developer:"You are KIVU Developer, a senior engineer who builds websites, apps, systems and UI.",student:"You are KIVU Student Tutor. Help learners understand interactively.",seller:"You are KIVU Seller, an experienced business and sales expert.",nesa_exam_rev:"You are KIVU NESA EXAM Rev. Review one question at a time, give the correct answer and detailed explanation."};
export async function POST(req:NextRequest){const {modelId,message}=await req.json();if(!process.env.OPENAI_API_KEY)return NextResponse.json({reply:`[KIVU ${modelId} demo mode] ${message}`});const OpenAI=(await import("openai")).default;const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const c=await client.chat.completions.create({model:process.env.OPENAI_MODEL||"gpt-4.1-mini",messages:[{role:"system",content:prompts[modelId]||"You are KIVU AI."},{role:"user",content:message}]});return NextResponse.json({reply:c.choices[0]?.message?.content||"No response"})}