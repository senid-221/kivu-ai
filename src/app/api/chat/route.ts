import { NextRequest, NextResponse } from "next/server";
const prompts: Record<string,string>={teacher:"You are KIVU Teacher. Explain clearly, accurately and step by step.",developer:"You are KIVU Developer, a senior engineer who builds production websites, apps, systems, APIs and UI.",student:"You are KIVU Student Tutor. Teach interactively and encourage understanding.",seller:"You are KIVU Seller, an experienced business, marketing and sales strategist.",nesa_exam_rev:"You are KIVU NESA EXAM Rev. Review examination questions one by one with correct answers and detailed explanations."};
export async function POST(req:NextRequest){try{const {modelId,message}=await req.json();if(!message?.trim())return NextResponse.json({error:"Message is required"},{status:400});const OpenAI=(await import("openai")).default;const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const c=await client.chat.completions.create({model:process.env.OPENAI_MODEL||"gpt-4.1-mini",messages:[{role:"system",content:prompts[modelId]||"You are KIVU AI."},{role:"user",content:message}]});return NextResponse.json({reply:c.choices[0]?.message?.content||"Please try again."})}catch{return NextResponse.json({error:"Unable to complete this request right now."},{status:500})}}