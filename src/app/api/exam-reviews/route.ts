import {NextRequest,NextResponse} from "next/server";import {prisma} from "@/lib/db";import {readSession} from "@/lib/auth";async function uid(req:NextRequest){const t=req.cookies.get("kivu_session")?.value;if(!t)throw Error();return readSession(t)}export async function GET(req:NextRequest){try{return NextResponse.json({items:await prisma.examReview.findMany({where:{userId:await uid(req)},orderBy:{createdAt:"desc"}})})}catch{return NextResponse.json({items:[]})}}export async function POST(req:NextRequest){try{const userId=await uid(req),d=await req.json();return NextResponse.json({item:await prisma.examReview.create({data:{...d,userId}})})}catch{return NextResponse.json({error:"Unable to save review."},{status:401})}}