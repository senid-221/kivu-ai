"use client";import {useEffect,useState} from "react";export default function Materials(){const[items,setItems]=useState<any[]>([]),[busy,setBusy]=useState(false);async function load(){const d=await fetch("/api/materials").then(r=>r.json());setItems(d.items||[])}useEffect(()=>{load()},[]);async function add(file:File){setBusy(true);try{const r=await fetch("/api/materials",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:file.name,type:file.type||"document",size:file.size})});if(r.ok)load()}finally{setBusy(false)}}return <main className="app"><header><div><b>✦ KIVU AI</b><small>Your learning materials</small></div></header><section className="welcome"><span>YOUR MATERIALS</span><h1>Keep your learning organized.</h1><p>Your materials are connected to your KIVU account.</p></section><label className="dropzone"><input type="file" onChange={e=>e.target.files?.[0]&&add(e.target.files[0])}/><b>{busy?"Adding material...":"＋ Add a material"}</b><small>Choose a document or learning file</small></label><section className="history">{items.map(x=><div className="historyItem" key={x.id}><b>📄 {x.name}</b><small>{x.type} · {Math.max(1,Math.round(x.size/1024))} KB · {new Date(x.createdAt).toLocaleDateString()}</small></div>)}{!items.length&&!busy&&<div className="empty"><p>No materials yet.</p></div>}</section></main>}