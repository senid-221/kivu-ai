"use client";import {useEffect,useState} from "react";export default function Profile(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[msg,setMsg]=useState("");useEffect(()=>{fetch("/api/auth/me").then(r=>r.json()).then(d=>{setName(d.user?.name||"");setEmail(d.user?.email||"")})},[]);async function save(){const r=await fetch("/api/auth/me",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name})});setMsg(r.ok?"Changes saved.":"Unable to save changes.")}return <main className="app"><header><div><b>✦ KIVU AI</b><small>Your profile</small></div></header><section className="welcome"><span>PROFILE</span><h1>Make KIVU yours.</h1><p>Personalize how KIVU supports your work and learning.</p></section><section className="studyBox"><h2>Your details</h2><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/><input value={email} disabled/><button className="button" onClick={save}>Save changes</button>{msg&&<p>{msg}</p>}</section></main>}