"use client";import{useState}from"react";export default function Seller(){const[message,setMessage]=useState(""),[businessType,setBusinessType]=useState("Retail / Shop"),[goal,setGoal]=useState("Increase sales"),[answer,setAnswer]=useState(""),[loading,setLoading]=useState(false);async function ask(){if(!message.trim())return;setLoading(true);setAnswer("");const r=await fetch("/api/seller",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message,businessType,goal})});const d=await r.json();setAnswer(d.answer||d.error);setLoading(false)}return <main className="app"><header><div><b>✦ KIVU AI</b><small>Seller Mode</small></div></header><section className="welcome"><span>YOUR BUSINESS ADVISOR</span><h1>Sell smarter. Grow stronger.</h1><p>Get practical advice for sales, marketing, pricing and business growth.</p></section><section className="studyBox"><div className="sellerControls"><select value={businessType} onChange={e=>setBusinessType(e.target.value)}><option>Retail / Shop</option><option>Online Business</option><option>Restaurant / Food</option><option>Service Business</option><option>Startup</option></select><select value={goal} onChange={e=>setGoal(e.target.value)}><option>Increase sales</option><option>Find more customers</option><option>Improve marketing</option><option>Set better prices</option><option>Build business strategy</option></select></div><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Tell KIVU Seller what is happening in your business..."/><button className="button" onClick={ask} disabled={loading}>{loading?"Analyzing your business...":"Ask KIVU Seller"}</button></section>{answer&&<section className="sellerAnswer"><h2>💼 KIVU Seller</h2><div>{answer}</div></section>}<section className="studyBox"><h2>Business help</h2><div className="suggestions">{["How can I increase my sales?","Help me set product prices","Create a marketing strategy","How should I talk to customers?"].map(x=><button key={x} onClick={()=>setMessage(x)}>{x}</button>)}</div></section></main>}