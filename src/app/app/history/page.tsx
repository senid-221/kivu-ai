"use client";import Link from "next/link";import {useEffect,useState} from "react";
export default function History(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{setItems(JSON.parse(localStorage.getItem("kivu-conversations")||"[]"))},[]);return <main className="app"><header><div><b>✦ KIVU AI</b><small>Your conversations</small></div><Link href="/app">New conversation</Link></header><section className="welcome"><span>HISTORY</span><h1>Your conversations</h1><p>Continue where you left off.</p></section><div className="history">{items.length?items.map((x,i)=><Link className="historyItem" key={i} href={"/app/chat?model="+x.model+"&conversation="+x.id}><b>{x.title}</b><small>{x.model} · {x.date}</small></Link>):<div className="empty"><div className="spark">✦</div><h2>No conversations yet</h2><p>Start a new conversation with any KIVU AI model.</p></div>}</div></main>