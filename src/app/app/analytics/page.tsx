"use client";import{useEffect,useState}from"react";export default function Analytics(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch("/api/analytics").then(r=>r.json()).then(setD)},[]);if(!d)return <main className="app"><h1>Loading analytics...</h1></main>;return <main className="app"><header><div><b>✦ KIVU AI</b><small>Learning analytics</small></div></header><section className="welcome"><span>LEARN SMARTER</span><h1>Your learning insights.</h1><p>{d.recommendation}</p></section><section className="progressGrid">{[["🎯",d.average+"%","Average score"],["📝",d.quizzes,"Quizzes"],["📚",d.studySessions,"Learning sessions"]].map(x=><div className="progressCard" key={x[2]}><span>{x[0]}</span><b>{x[1]}</b><small>{x[2]}</small></div>)}</section><section className="studyBox"><h2>📈 Quiz performance</h2>{d.timeline.length?<div className="activityList">{d.timeline.map((x:any)=><div className="activity" key={x.date}><b>{x.date}</b><span>{x.score}%</span><small>{x.count} quiz session{x.count>1?"s":""}</small></div>)}</div>:<p>Complete quizzes to see your performance trend.</p>}</section><section className="studyBox"><h2>🧠 Smart revision plan</h2><div className="activityList">{d.plan.map((x:any)=><div className="activity" key={x.day}><b>{x.day}</b><small>{x.task}</small></div>)}</div></section></main>}