"use client";import{useState}from"react";export default function Teacher(){const[message,setMessage]=useState(""),[subject,setSubject]=useState("General"),[level,setLevel]=useState("Secondary school"),[answer,setAnswer]=useState(""),[loading,setLoading]=useState(false);async function ask(){if(!message.trim())return;setLoading(true);setAnswer("");const r=await fetch("/api/teacher",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message,subject,level})});const d=await r.json();setAnswer(d.answer||d.error);setLoading(false)}return <main className="app"><header><div><b>✦ KIVU AI</b><small>Teacher Mode</small></div></header><section className="welcome"><span>YOUR AI TEACHER</span><h1>Learn as if you are with a real teacher.</h1><p>Ask questions, get step-by-step explanations, examples and practice.</p></section><section className="studyBox"><div className="teacherControls"><select value={subject} onChange={e=>setSubject(e.target.value)}><option>General</option><option>Mathematics</option><option>Biology</option><option>Chemistry</option><option>Physics</option><option>English</option><option>History</option></select><select value={level} onChange={e=>setLevel(e.target.value)}><option>Primary school</option><option>Secondary school</option><option>University</option></select></div><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Ask your teacher anything..."/><button className="button" onClick={ask} disabled={loading}>{loading?"Preparing your lesson...":"Ask KIVU Teacher"}</button></section>{answer&&<section className="teacherAnswer"><h2>👩🏾‍🏫 KIVU Teacher</h2><div>{answer}</div></section>}<section className="studyBox"><h2>Try asking</h2><div className="suggestions">{["Explain this topic step by step","Give me an example","Test my understanding","Help me revise for an exam"].map(x=><button key={x} onClick={()=>setMessage(x)}>{x}</button>)}</div></section></main>}