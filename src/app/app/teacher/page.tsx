"use client";
import{useState}from"react";
export default function Teacher(){
 const[message,setMessage]=useState(""),[subject,setSubject]=useState("Rusange"),[level,setLevel]=useState("Amashuri yisumbuye"),[answer,setAnswer]=useState(""),[loading,setLoading]=useState(false);
 async function ask(){if(!message.trim())return;setLoading(true);setAnswer("");const r=await fetch("/api/teacher",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message,subject,level})});const d=await r.json();setAnswer(d.answer||d.error);setLoading(false)}
 return <main className="app"><header><div><b>EDUKA</b><small>Mwarimu • Teacher</small></div></header><section className="welcome"><span>UMWARIMU WAWE WA AI</span><h1>Iga nk'uri kumwe n'umwarimu.</h1><p>Ask questions and get clear, step-by-step explanations.</p></section><section className="studyBox"><div className="teacherControls"><select value={subject} onChange={e=>setSubject(e.target.value)}><option>Rusange</option><option>Imibare</option><option>Ibinyabuzima</option><option>Chimie</option><option>Fizika</option><option>Icyongereza</option><option>Amateka</option></select><select value={level} onChange={e=>setLevel(e.target.value)}><option>Amashuri abanza</option><option>Amashuri yisumbuye</option><option>Kaminuza</option></select></div><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Baza umwarimu ikibazo icyo ari cyo cyose..."/><button className="button" onClick={ask} disabled={loading}>{loading?"Ndimo gutegura isomo...":"Baza EDUKA Mwarimu"}</button></section>{answer&&<section className="teacherAnswer"><h2>EDUKA Mwarimu</h2><div>{answer}</div></section>}<section className="studyBox"><h2>Kwamamaza? Oya — Gerageza kubaza</h2><div className="suggestions">{["Sobanura iri somo intambwe ku yindi","Mpa urugero","Gerageza kureba niba nasobanukiwe","Mfasha gusubiramo ikizamini"].map(x=><button key={x} onClick={()=>setMessage(x)}>{x}</button>)}</div></section></main>}