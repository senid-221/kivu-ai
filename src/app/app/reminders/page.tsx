"use client";import{useEffect,useState}from"react";export default function Reminders(){const[r,setR]=useState<any[]>([]),[title,setTitle]=useState("Study with KIVU AI"),[time,setTime]=useState("19:00"),[days,setDays]=useState("Every day");const load=()=>fetch("/api/reminders").then(x=>x.json()).then(setR);useEffect(()=>{load()},[]);const add=async()=>{await fetch("/api/reminders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title,time,days})});load()};const toggle=async(x:any)=>{await fetch("/api/reminders",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:x.id,enabled:!x.enabled})});load()};return <main className="app"><header><div><b>✦ KIVU AI</b><small>Study reminders</small></div></header><section className="welcome"><span>STAY CONSISTENT</span><h1>Build a study routine.</h1><p>Set a schedule and keep your learning momentum.</p></section><section className="studyBox"><div className="reminderForm"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Reminder title"/><input type="time" value={time} onChange={e=>setTime(e.target.value)}/><select value={days} onChange={e=>setDays(e.target.value)}><option>Every day</option><option>Weekdays</option><option>Weekends</option></select><button className="button" onClick={add}>Add reminder</button></div></section><section className="studyBox"><h2>🔔 Your reminders</h2>{r.length?r.map(x=><div className="reminder" key={x.id}><div><b>{x.title}</b><small>{x.time} · {x.days}</small></div><button className="button" onClick={()=>toggle(x)}>{x.enabled?"On":"Off"}</button></div>):<p>No reminders yet.</p>}</section></main>}