"use client";
import {useSearchParams} from "next/navigation";import {useState} from "react";
const names:any={teacher:"Teacher",developer:"Developer",student:"Student",seller:"Seller",nesa_exam_rev:"NESA EXAM Rev"};
export default function Chat(){const q=useSearchParams();const model=q.get("model")||"teacher";const [messages,setMessages]=useState<any[]>([]);const [input,setInput]=useState("");
async function send(){if(!input)return;const msg=input;setMessages(x=>[...x,{role:"user",content:msg}]);setInput("");const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({modelId:model,message:msg})});const d=await r.json();setMessages(x=>[...x,{role:"assistant",content:d.reply}])}
return <main className="chat"><header><b>KIVU {names[model]||"AI"}</b><small>{model==="nesa_exam_rev"?"Exam questions reviewed one by one with detailed explanations":"Your specialized AI assistant"}</small></header>{model==="nesa_exam_rev"&&<div className="exam">📝 NESA EXAM MODE · Question-by-question review</div>}<div className="messages">{messages.length===0?<div className="empty"><h1>How can KIVU {names[model]} help?</h1><p>Start a conversation below.</p></div>:messages.map((m,i)=><div className={"message "+m.role} key={i}>{m.content}</div>)}</div><div className="composer"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask anything..."/><button onClick={send}>Send ↑</button></div></main>}