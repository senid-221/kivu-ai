import {SignJWT,jwtVerify} from "jose";const key=()=>new TextEncoder().encode(process.env.AUTH_SECRET||"kivu-ai-session");export async function createSession(userId:string){return new SignJWT({userId}).setProtectedHeader({alg:"HS256"}).setIssuedAt().setExpirationTime("30d").sign(key())}export async function readSession(token:string){const {payload}=await jwtVerify(token,key());return payload.userId as string}