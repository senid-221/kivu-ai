import {SignJWT,jwtVerify} from "jose";function key(){const secret=process.env.AUTH_SECRET;if(!secret&&process.env.NODE_ENV==="production")throw new Error("Server authentication is not configured.");return new TextEncoder().encode(secret||"kivu-ai-development-session")}export async function createSession(userId:string){return new SignJWT({userId}).setProtectedHeader({alg:"HS256"}).setIssuedAt().setExpirationTime("30d").sign(key())}export async function readSession(token:string){const {payload}=await jwtVerify(token,key());if(typeof payload.userId!=="string")throw new Error("Invalid session.");return payload.userId}